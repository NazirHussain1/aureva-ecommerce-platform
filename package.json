{
  "name": "aureva",
  "private": true,
  "scripts": {
    "dev": "node scripts/start-dev.js",
    "dev:backend": "npm --prefix backend start",
    "dev:frontend": "npm --prefix aureva-frontend run dev -- --host 127.0.0.1 --port 4173 --strictPort",
    "build:frontend": "npm --prefix aureva-frontend run build",
    "test:backend": "npm --prefix backend test",
    "setup:categories": "npm --prefix backend run setup:categories"
  }
}
